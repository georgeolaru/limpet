# Claude Code / agent scaffolding (contains local runbook + private host notes)
.claude/

# macOS
.DS_Store

# Logs / build artifacts
*.log

# Release output from release-app.sh (notarized .zip; ships via GitHub Releases)
dist/
